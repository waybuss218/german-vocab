# German Vocab Review

GitHub Pages flat deployment build.

Upload these five files to the repository root:
- index.html
- app.js
- style.css
- vocab.json
- README.md

No folders are required.

The app's current data is embedded in `index.html`; `vocab.json` is included as the source data copy.
