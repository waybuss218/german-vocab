# 德福考前必备-词汇 · GitHub 替换包

本包使用最终词库整合文件 `vocab-data.json`，网站运行时不再读取旧的 `vocab.json`。

请上传并覆盖：`index.html`、`app.js`、`style.css`、`vocab-data.json`。

保留你 GitHub 现有的 `supabase-config.js`，不要覆盖。

确认新版文件都上传成功后，再删除旧的 `vocab.json`。
